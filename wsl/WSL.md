# Instalando o WSL

Vá em Recursos do Windows
- Garanta que tenha selecionado "Plataforma de Máquina Virtual" e "Subsistema do Windows para Linux"

Rode no powershell como admin e execute os seguintes comandos um após o outro

- `dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart`

- `dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart`

- `wsl.exe --update`


## Desinstale todas as distros caso tenha alguma instalada
Como desinstalar uma distribuição WSL?
Execute `wsl --unregister <distroName>` em que <distroName> é o nome da distribuição do Linux, que pode ser visto na lista no `wsl -l`.

Instale o update.msi baixando por esse [link!](https://drive.google.com/file/d/1A7jydq_vpzIe4GidBib4TrCmvf_0G3xS/view?usp=drive_link)

## Instalando a Distro
Use a loja da MS pra baixar o Ubuntu ou a distro que desejar. Preferencialmente Ubuntu.

Abra a o WSL. Pode ser pelo "Abrir na loja" ou abra o um terminal powershell e escreva WSL.
Recomendo usar o [Microsoft Terminal](https://learn.microsoft.com/pt-br/windows/terminal/install)

Se o WSL por qualquer motivo iniciar em root e você desejar que inicie como seu user, use o comando no powershell
ubuntu config --default-user <seu-user>

Antes de começar a utilizar de um `ping 8.8.8.8`, consulte problemas de conexão.

### Problemas de conexão
Execute o comando no ubuntu:
`sudo nano /etc/resolv.conf`

```
# This file was automatically generated by WSL. To stop automatic generation of this file, add the following entry to />
# [network]
# generateResolvConf = false
nameserver 8.8.8.8
nameserver 8.8.4.4
```

Insira aessa informação no arquivo e salve com ctrl+x, y, enter. Execute o ping novamente

### Caso não pingar, faça um roteador com seu celular e conecte seu notebook e execute o ping.

Caso após essas duas soluções não pingar... De um Google :)